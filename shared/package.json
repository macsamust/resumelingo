{
  "name": "@resumelingo/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Type definitions shared between server/ (Node+Express+Postgres) and worker/ (Cloudflare Workers+Hono+D1) — the DB record shapes, catalog/config value types, and auth token payloads that need to stay identical across both backends. Not consumed by client/, which intentionally keeps its own API-facing view types (see client/src/types/index.ts).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
