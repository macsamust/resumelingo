# Only needed for split local dev (Vite on :5173 talking to a separately
# running `wrangler dev` on :8787). Leave unset for production — the
# deployed Worker serves the client and /api/* from the same origin.
VITE_API_URL=http://localhost:8787/api
