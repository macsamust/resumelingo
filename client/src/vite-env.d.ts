/// <reference types="vite/client" />

/** Injected by vite.config.ts's `define` — the deployed build's short git hash. */
declare const __APP_VERSION__: string;
/** Injected by vite.config.ts's `define` — the ISO timestamp this bundle was built. */
declare const __APP_BUILD_DATE__: string;
