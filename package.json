{
  "name": "resumelingo-app",
  "private": true,
  "version": "1.0.0",
  "description": "ResumeLingo — client/ (React+Vite) paired with worker/ (Cloudflare Workers+Hono+D1), the only backend. The earlier server/ (Node+Express+Postgres) parity backend was retired and removed — see git history before this commit if you ever need to reference it.",
  "scripts": {
    "build:shared": "npm install --prefix shared && npm run build --prefix shared",
    "dev:client": "npm run dev --prefix client",
    "install:worker": "npm run build:shared && npm install --prefix worker && npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "dev:worker": "npm run build:shared && npm run dev --prefix worker",
    "deploy:worker": "npm run build:shared && npm run build --prefix client && npm run deploy --prefix worker",
    "deploy": "npm run deploy:worker",
    "preview": "npm run dev:worker"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
